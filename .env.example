# This file contains default environment variables for development
# Railway will override these with its own environment variables in production

# Flask Settings
FLASK_APP=wsgi.py
FLASK_ENV=development

# Server Settings
PORT=5000

# CORS Settings
ALLOWED_ORIGIN=*
ENVIRONMENT=development
